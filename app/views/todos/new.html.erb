<%= link_to "Back", :back %>

<%= form_for @todo do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :is_completed %>
  <%= f.check_box :is_completed %>

  <%= f.submit %>
<% end %>
