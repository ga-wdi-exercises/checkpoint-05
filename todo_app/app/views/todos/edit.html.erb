<h2>Edit Todo item</h2>
<%= form_for @todo do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content, :size => 60 %>

  <%= f.label :is_completed %>
  <%= f.text_field :is_completed %>

  <%= f.submit "Edit" %>
<% end %>
